package module_4A.Tasks;


public interface Task_17B{
    //All testers in your unit must test that the correct username is entered,
    // correct password that matches the username and that Login to the homePage or
    // dashboard is successful. As the test Lead, create an interface that the testers should.
    public abstract void password(String string);
    public abstract void username(String string);
}
